<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand text-light" href="{{ path('home') }}"><img class="logo" src="{{ asset('build/images/Vintage-Tristan-Fernandez-1000px.png') }}" alt="" srcset="" /></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item d-flex">
            <a class="nav-link active text-light p-4" aria-current="page" href="{{ path('home') }}">Accueil</a>
            {# <a class="nav-link active text-light p-4" aria-current="page" href="{{ path('home') }}">Présentation</a>
						<a class="nav-link active text-light p-4" aria-current="page" href="{{ path('home') }}">Portfolio</a>
						<a class="nav-link active text-light p-4" aria-current="page" href="{{ path('home') }}">Blog</a>
						<a class="nav-link active text-light p-4" aria-current="page" href="{{ path('home') }}">Contact</a> #}
          </li>
        </ul>
      </div>
      {% if app.user %}
        {% if is_granted('ROLE_ADMIN') %}
          {# <div class="dropdown me-2">
            <button class="btn btn-secondary dropdown-toggle text-light text-bold" type="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="bi bi-wrench-adjustable"></i>
              Admin
            </button>
            <ul class="dropdown-menu">
              <li>
                <a class="dropdown-item" href="{{ path('admin.user.index') }}">Gérer les utilisateurs</a>
              </li>
              <li>
                <a class="dropdown-item" href="{{ path('admin') }}">Article</a>
              </li>
              <li>
                <a class="dropdown-item" href="{{ path('app_categorie_index') }}">Catégories</a>
              </li>
            </ul>
          </div> #}
        {% endif %}
        <div class="navbar-btn me-2">
          {# <a href="{{ path('app.user.compte') }}" class="btn btn-secondary text-light text-bold">
            <i class="bi bi-person-badge"></i>
            Compte
          </a> #}
        </div>
        <div class="navbar-btn">
          {# <a href="{{ path('logout') }}" class="btn btn-danger text-light text-bold"> #}
          <i class="bi bi-door-open-fill"></i>
          Se déconnecter
          {# </a> #}
        </div>
      {% else %}
        <div class="navbar-btn me-2">
          <a href="{{ path('login') }}" class="btn btn-secondary text-light text-bold">
            <i class="bi bi-door-closed-fill"></i>
            Login
          </a>
        </div>
        <div class="navbar-btn me-2">
          {# <a href="{{ path('register') }}" class="btn btn-secondary text-light text-bold"> #}
          <i class="bi bi-person-plus-fill"></i>
          SignUp
          {# </a> #}
        </div>
      {% endif %}
    </div>
  </nav>
</header>
