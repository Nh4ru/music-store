<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-light personnal-border">
    <div class="container-fluid">
      <a class="navbar-brand text-light" href="{{ path('home') }}"><img class="logo" src="{{ asset('build/images/Vintage-Tristan-Fernandez-1000px.png') }}" alt="" srcset="" /></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item d-flex">
            <a class="nav-link text-primary text-uppercase active p-4" aria-current="page" href="{{ path('home') }}">Accueil</a>
          </li>
        </ul>
      </div>
      {% if app.user %}
        {# {% if is_granted('ROLE_ADMIN') %}
          <div class="navbar-btn me-2">
            <a href="{{ path('admin') }}" class="btn btn-secondary">Admin</a>
          </div>
        {% endif %}
        <div class="navbar-btn">
          <a href="{{ path('logout') }}" class="btn btn-danger">Se deconnecter</a>
        </div>
      {% else %} #}
        <div class="navbar-btn">
          <a href="{{ path('login') }}" class="btn btn-primary text-light"><i class="bi bi-door-closed me-2"></i>Login</a>
        </div>
      {% endif %}
    </div>
  </nav>
</header>
